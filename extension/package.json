{
  "name": "bookmark-sync-extension",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "mkdir -p dist",
    "build": "tsc && cp -r public/* dist/ && npm run convert-icons",
    "convert-icons": "npx svgexport public/icons/icon16.svg dist/icons/icon16.png 16:16 && npx svgexport public/icons/icon16.svg dist/icons/icon48.png 48:48 && npx svgexport public/icons/icon16.svg dist/icons/icon128.png 128:128",
    "watch": "tsc -w",
    "package": "cd dist && zip -r ../bookmark-sync-extension.zip *"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "svgexport": "^0.4.2",
    "typescript": "^5.3.3"
  }
}